[project]
name = "phase-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "name", email = "v.longlh24@vinfast.vn" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
phase-1 = "phase_1:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
